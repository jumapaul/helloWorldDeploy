apiVersion: v1
kind: Service
metadata:
  name: "hello-world-2-service"
spec:
  selector:
    app: hello-world-2
  type: NodePort
  ports:
    - protocol: TCP
      port: 80  # Port ingress targets
      targetPort: 30133